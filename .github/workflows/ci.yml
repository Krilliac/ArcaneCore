name: CI

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Restore
        run: dotnet restore ArcaneCore.slnx

      # Directory.Build.props sets TreatWarningsAsErrors=true, so a warning fails the build.
      - name: Build
        run: dotnet build ArcaneCore.slnx -c Release --no-restore

      - name: Test
        run: dotnet test ArcaneCore.slnx -c Release --no-build --verbosity normal
